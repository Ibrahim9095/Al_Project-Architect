{
  "name": "ai-project-architect",
  "version": "1.0.0",
  "private": true,
  "description": "AI Project Architect \u2014 Analysis Engine",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate:analysis": "npm run build && node scripts/validate-analysis-engine.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "type": "module"
}
